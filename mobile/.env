# Copy to .env for local web/dev, or rely on eas.json env for builds.
# Point the app at your deployed backend (Render URL) so the APK works anywhere.
EXPO_PUBLIC_API_URL=https://spotify-clone-1-ln6l.onrender.com
